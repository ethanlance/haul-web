{{#if product.getCollections.collections }}
	<h4 class="top-border padding-top">Featured In</h4>
{{/if}}


{{#with currentUser as currentUser}}
	{{#each product.getCollections.collections}}
		{{collection-badge collection=this currentUser=currentUser}}
	{{/each}}				
{{/with}}