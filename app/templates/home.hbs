{{preserve-scroll currentScrollPos=currentScrollPos}}

<div class="container-fluid no-margins">
	<div class="haul-content">

		<div class="hamburger-right-side">

			{{!-- HAMBURGER ICON, WHEN LEFT NAV IS COLAPSED --}}
			{{!-- HAMBURGER ICON, WHEN LEFT NAV IS COLAPSED --}}
			{{!-- HAMBURGER ICON, WHEN LEFT NAV IS COLAPSED --}}
			{{!-- HAMBURGER ICON, WHEN LEFT NAV IS COLAPSED --}}
			{{hamburger-menu}}
			<input type="checkbox" id="hamburger-trigger" class="hamburger" />
			<label for="hamburger-trigger" class="with-page-header"></label>
		

			{{!-- FLUID LEFT --}}
			{{!-- FLUID LEFT --}}
			{{!-- FLUID LEFT --}}
			{{!-- FLUID LEFT --}}
			<div class="haul-fluid-left" data-info="haul-fluid-left"> 
				
				<div class="haul-feed-content">
				

				{{!-- FEED --}}
				{{!-- FEED --}}
				{{!-- FEED --}}
				{{!-- FEED --}}
				{{!-- FEED --}}
				{{!-- FEED --}} 
				{{#each post in sortedContent}}
					{{partial 'feed_post'}}	
				{{/each}} 
				{{#infinite-scroll content=pagedContent hasMore=paginateHasMore }}
					<i class="fa fa-circle-o-notch fa-spin"></i>
				{{/infinite-scroll}}


	 
				</div> 
			</div>



			{{!-- FIXED RIGHT --}}
			{{!-- FIXED RIGHT --}}
			{{!-- FIXED RIGHT --}}
			{{!-- FIXED RIGHT --}}
			<div class="haul-fixed-right">
				<div class="haul-fixed-content">
					<div class="message-center">

						<h2>NOTIFICATIONS</h2>
					
						{{mention-pod goToRoute="goToRoute"}}
					</div>
				</div>
			</div>
		</div> 
	</div>
</div>