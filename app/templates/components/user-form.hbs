        
<div {{bind-attr class="showForm:show:hide"}}>
    {{#form-for model}}
    <div >
        {{input-field model.firstname class="form-control" placeholder="First Name" label="false" enter='btnClick'}}
        {{input-field model.lastname class="form-control" placeholder="Last Name" label="false" enter='btnClick'}}
    </div> 
    {{/form-for}}

    <div {{bind-attr class="showErrorMessage:show:hide :errors"}}>
        <span class="alert">{{{errorMessage}}}</span>
    </div>

    <div>

        {{ladda-btn 
            name='SAVE'
            btnClasses='btn ladda-button btn-right btn-positive'
            btnClick='saveForm'
            spin=isProcessing
        }}

    </div>
</div>    




<div {{bind-attr class="showForm:hide:show"}}>
    <h4>You're new name! {{session.currentUser.firstname}} {{session.currentUser.lastname}}</h4>
    <button {{action 'showForm'}} class="btn btn-default btn-right btn-positive">Change again</button>
</div>