<div class="row padding-top comments">
	<div class="col-md-12 col-sm-12 col-xs-12">
		
		{{!-- COMMENT FORM --}}
		<div class="row" id="leaveComment">
			<div class="col-md-12 col-sm-12 col-xs-12 ">
			<h4>Comments</h4>
			{{#form-wrapper}}

				{{input comment as="text" canShowValidationError='true' placeholder="add a comment..." class="commentbox"}}
				
	    		<div {{bind-attr class="showServerErrors:show:hide :alert"}}>{{{serverErrorMessage}}}</div>
	  			
				<div class="text-right">
					
					{{ladda-btn
						name='post comment'
						class="display-inline"
						btnClasses='btn btn-default ladda-button'
						btnClick='submit'
						spin=isProcessing
					}}

				</div>
			{{/form-wrapper}}
			</div>
		</div>



		{{!-- COMMENTS --}}
		<div class="row">
			<div class="col-md-12 col-sm-12 col-xs-12">


				{{#each item in sortedComments }}
				<div class="comment">
					<div {{bind-attr class="canDelete:show:hide"}}>
						<div {{action "delete" item}}class="btn btn-default btn-sm btn-delete"><span class="glyphicon glyphicon-remove"></span></div>
					</div>
					<div class="image">
						{{#link-to 'profile' item.user.username }}{{ make-icon icon=item.user.icon isSmall=true}}{{/link-to}}
					</div>
					<div class="single-comment">
						<span class="name">{{#link-to 'profile' item.user.username }}{{item.user.name}}{{/link-to}}</span>
						<span class="comment-string">{{sanitize-html item.comment "comment"}}</span>
						<span class="moment">{{ format-date item.created_at }}</span>
					</div>
				</div>
				{{/each}}
		
			</div>
		</div>
		<div class="row">
			<div class="col-md-12 col-sm-12 col-xs-12 text-center">

				<button {{action 'fetchMore'}} {{bind-attr class=":btn :btn-default hasMore::disabled"}}>load more</button>

			</div>
		</div>
	</div>
</div>